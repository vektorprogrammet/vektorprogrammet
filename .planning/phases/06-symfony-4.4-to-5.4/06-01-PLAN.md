<plan id="06-01">
  <task id="1" status="done">
    <description>Upgrade composer.json to Symfony 5.4.*, PHP >=8.0, PHPUnit ^9.5</description>
    <files>composer.json, composer.lock</files>
    <verification>composer install succeeds, cache:clear works</verification>
  </task>

  <task id="2" status="done">
    <description>Migrate User entity: AdvancedUserInterface → UserInterface + PasswordAuthenticatedUserInterface, getRoles() returns string[], add getUserIdentifier(), __serialize/__unserialize</description>
    <files>src/App/Entity/User.php, src/App/Entity/Role.php</files>
    <verification>User implements correct interfaces, Role standalone</verification>
  </task>

  <task id="3" status="done">
    <description>Update security config: encoders→password_hashers, anonymous→lazy, add user_checker and entry_point</description>
    <files>config/security.yml</files>
    <verification>Security config loads without errors</verification>
  </task>

  <task id="4" status="done">
    <description>BaseController → AbstractController with getSubscribedServices() for 35 services</description>
    <files>src/App/Controller/BaseController.php, src/App/Controller/*.php</files>
    <verification>Controllers compile, no container errors</verification>
  </task>

  <task id="5" status="done">
    <description>Flip event dispatch signature (27 calls), update event base classes (11 files), rename kernel events</description>
    <files>src/App/Event/*.php, src/App/EventSubscriber/*.php, src/App/Controller/*.php</files>
    <verification>Events dispatch correctly</verification>
  </task>

  <task id="6" status="done">
    <description>PHPUnit 9 compat: setUp/tearDown :void, Client→KernelBrowser, FieldTypeHelpExtension static method</description>
    <files>tests/**/*.php, src/App/Form/Extension/FieldTypeHelpExtension.php</files>
    <verification>PHPUnit bootstrap works</verification>
  </task>

  <task id="7" status="done">
    <description>Register controllers + services in services.yml autodiscovery. Fix Sf5 form/template/test compat issues.</description>
    <files>config/services.yml, tests/bootstrap.php, src/App/Form/Type/*.php, templates/profile/profile_header.html.twig, templates/questions/repeatable_question.html.twig, tests/**/*.php</files>
    <action>
      - Expanded services.yml autodiscovery: {Controller,EventSubscriber,Form,Google,Mailer,Role,Security,Service,Sms,Twig,Validator}
      - Fixed bootstrap.php: fallback to .env.test when .env missing
      - Fixed FeedbackType form name with space
      - Added html5:false to 11 DateTimeType fields across 8 form files
      - Fixed profile_header.html.twig: user.roles → user.roleEntities
      - Fixed repeatable_question.html.twig: form_rest() to prevent double-render
      - PHPUnit 9 fixes: assertStringContainsString, mock namespace, UploadedFile
      - Test fixes: RoleManagerTest, UserEntityUnitTest, GeoLocationTest, form field values
    </action>
    <verification>
      - 496 tests: 2 errors, 12 failures (down from 19 errors, 31 failures)
      - Remaining failures are: pre-existing (AccessRule 404, receipt null asset), environment (iconv locale), deferred (PasswordReset/SwiftMailer), template double-render (interview schema)
    </verification>
  </task>

  <task id="8" status="current">
    <description>Evaluate remaining failures, update CLAUDE.md, commit Sprint 6</description>
    <files>CLAUDE.md, .planning/STATE.md</files>
    <action>
      - Verify remaining 14 failures are acceptable (pre-existing/env/deferred)
      - Update CLAUDE.md: PHP 8.4 for tests, new baseline failure count
      - Commit all Sprint 6 changes
    </action>
    <verification>Clean commit, tests within acceptable range</verification>
  </task>
</plan>

<execution-strategy>
  <wave id="1" mode="sequential">
    <tasks>1, 2, 3, 4, 5, 6</tasks>
    <notes>All done. Core Sf5 compatibility changes.</notes>
  </wave>
  <wave id="2" mode="sequential">
    <tasks>7, 8</tasks>
    <notes>Task 7 done. Task 8 remaining: evaluate failures + commit.</notes>
  </wave>
</execution-strategy>

{
  "name": "vektorprogrammet",
  "version": "1.0.0",
  "description": "This is the source code for vektorprogrammets new homepage.",
  "main": " ",
  "dependencies": {
    "@coreui/coreui": "2.0.18",
    "@fortawesome/fontawesome-free": "5.4.1",
    "axios": "^0.18.0",
    "bootstrap": "4.1.3",
    "ckeditor": "4.5.10",
    "cropperjs": "1.4.1",
    "dropzone": "4.3.0",
    "jquery": "3.3.1",
    "moment": "2.22.2",
    "popper.js": "1.14.4"
  },
  "devDependencies": {
    "babel-core": "6.26.3",
    "babel-preset-env": "1.7.0",
    "concurrently": "3.6.1",
    "gulp": "4.0.0",
    "gulp-autoprefixer": "3.1.1",
    "gulp-babel": "7.0.1",
    "gulp-changed": "1.3.2",
    "gulp-concat": "2.6.1",
    "gulp-cssnano": "2.1.3",
    "gulp-imagemin": "4.1.0",
    "gulp-plumber": "1.2.0",
    "gulp-sass": "4.0.1",
    "gulp-uglify": "1.5.4",
    "node-sass": "^4.12.0"
  },
  "scripts": {
    "test": "./bin/phpunit --configuration app/phpunit.xml.dist",
    "test:win": ".\\bin\\phpunit --configuration app/phpunit.xml.dist",
    "setup": "php ./composer.phar install -n && npm install && npm run setup:scheduling && npm run build:dev && php bin/console doctrine:schema:drop --force --env=dev && php bin/console doctrine:schema:create --env=dev && php bin/console doctrine:schema:update --force --env=dev && php bin/console doctrine:fixtures:load --env=dev -n",
    "setup:win": "php .\\composer.phar install -n && npm install && npm run setup:scheduling:win && npm run build:dev && php bin/console doctrine:schema:drop --force --env=dev && php bin/console doctrine:schema:create --env=dev && php bin/console doctrine:schema:update --force --env=dev && php bin/console doctrine:fixtures:load --env=dev -n",
    "setup:scheduling": "cd src/AppBundle/AssistantScheduling/Webapp && npm install",
    "setup:scheduling:win": "cd src\\AppBundle\\AssistantScheduling\\Webapp && npm install",
    "start": "concurrently --kill-others \"php bin/console server:run --docroot=web 0.0.0.0:8000\" \"npm run watch\"",
    "setup:client": "cd client && npm install",
    "watch": "gulp",
    "watch:scheduling": "gulp watch:scheduling",
    "db:reload": "npm run db:update",
    "db:update": "php bin/console doctrine:schema:update --force --env=dev && php bin/console doctrine:fixtures:load -n --env=dev && php bin/console doctrine:migrations:version -n --delete --all --env=dev && php bin/console doctrine:migrations:version -n --add --all --env=dev",
    "build": "gulp build:dev",
    "compress-images": "gulp compressImages",
    "build:scheduling": "cd src/AppBundle/AssistantScheduling/Webapp && npm run build:dev",
    "build:scheduling:win": "cd src\\AppBundle\\AssistantScheduling\\Webapp && npm run build:dev",
    "build:scheduling:files": "gulp build:scheduling",
    "build:client": "gulp build:client",
    "build:dev": "gulp build:dev",
    "build:prod": "gulp build:prod",
    "code-style": "./bin/php-cs-fixer fix src/ --dry-run --diff -vv",
    "code-style:win": ".\\bin\\php-cs-fixer fix src/ --dry-run --diff -vv",
    "cs": "./bin/php-cs-fixer fix src/ -vv || .\\bin\\php-cs-fixer fix src/ -vv",
    "cs:win": ".\\bin\\php-cs-fixer fix src/ -vv"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vektorprogrammet/webpage.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/vektorprogrammet/webpage/issues"
  },
  "homepage": "https://github.com/vektorprogrammet/webpage#readme"
}

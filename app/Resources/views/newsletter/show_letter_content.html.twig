{% extends 'adminBase.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h2>{{ letter.title }}</h2>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            {{ letter.content|replace({'\n':'<br>'})|safe_html }}
        </div>
    </div>
{% endblock %}

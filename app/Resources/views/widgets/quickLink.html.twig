(quickLink.html.twig)


<div class="col-4">
    {% for quicklink in quickLinks %}
        {{quicklink.getUrl()}}
    {% endfor %}
</div>
<!--=======-->
<div class="col-12">


    <div class="card" >
        <div class="card-header">
            <div class="card-title">
                QuickLinks
            </div>
        </div>
        <div class="card-body">
            <div class="row">

                {% for quicklink in quickLinks %}
                    <div class="col-4">f
                        {{ quicklink.getTitle() }} <br>
                        <a href="{{quicklink.getUrl()}}"><img src="{{quicklink.getIconUrl()}}" alt="Icon" width="42" height="42"></a>
                    </div>

                {%  endfor %}
            </div>
        </div>

    </div>
</div>
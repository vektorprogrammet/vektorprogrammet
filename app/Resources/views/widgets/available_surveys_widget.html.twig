<link rel="stylesheet" href="{{ asset_with_version('css/pages/widgets.css') }}">
{% for survey in availableSurveys  %}

<div class="col-12 col-md-6 col-xxxl-4">
    <div class="card text-center">
        <div class="card-header">
            <i class="fa  fa-comment"></i> Tilgjenglig undersøkelse
        </div>
        <div class="card-body">
            <h5 class="card-title">{{ survey.name | safe_html }}</h5>
            <p class="card-text"> Om du ikke svarer på denne undersøkelsen spår jeg deg ett liv med sur kaffe.</p>
            <a href="{{ path('survey_show_team',{'id':survey.id}) }}" target="_blank" class="btn btn-primary">
                {{survey.surveyPopUpMessage|safe_html|raw}}</a>
        </div>
        <div class="card-footer text-muted">
            Vår 2029 | - 3650 dager siden
        </div>

    </div>
</div>

<div class="col-12 col-md-6 col-xxxl-4">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title"><i class="fa  fa-comment"></i>  {{ survey.name | safe_html | raw}}</h5>
            <p class="text-muted"> Vår 2029 | - 3650 dager siden </p>
            <p class="card-text"> Om du ikke svarer på denne undersøkelsen spår jeg deg ett liv med sur kaffe.</p>
            <div href="{{ path('survey_show_team',{'id':survey.id}) }}" target="_blank" class="btn btn-primary survey-widget-btn">
                {{ survey.surveyPopUpMessage|safe_html|raw}}
            </div>
        </div>
    </div>
</div>

<div class="col-12 col-md-6 col-xxxl-4">
    <div class="card">
        <div class="card-header">
            <i class="fa  fa-comment"></i> Tilgjenglig undersøkelse
        </div>
        <div class="card-body">
            <h5 class="card-title">{{ survey.name | safe_html | raw}}</h5>
            <p class="text-muted"> Vår 2029 | - 3650 dager siden </p>
            <p class="card-text"> Om du ikke svarer på denne undersøkelsen spår jeg deg ett liv med sur kaffe.</p>
            <div href="{{ path('survey_show_team',{'id':survey.id}) }}" target="_blank" class="btn btn-primary survey-widget-btn">
                {{ survey.surveyPopUpMessage|safe_html|raw}}
            </div>
        </div>
    </div>
</div>

    <div class="col-12 col-md-6 col-xxxl-4">
        <div class="card">
            <div class="card-header text-center">
                <i class="fa  fa-comment"></i> Tilgjenglig undersøkelse
            </div>
            <div class="card-body">
                <h5 class="card-title">{{ survey.name | safe_html | raw}}</h5>
                <p class="text-muted"> Vår 2029 | - 3650 dager siden </p>
                <p class="card-text"> Om du ikke svarer på denne undersøkelsen spår jeg deg ett liv med sur kaffe.</p>
                <div href="{{ path('survey_show_team',{'id':survey.id}) }}" target="_blank" class="btn btn-primary survey-widget-btn">
                    {{ survey.surveyPopUpMessage|safe_html|raw}}
                </div>
            </div>
        </div>
    </div>


{% endfor %}

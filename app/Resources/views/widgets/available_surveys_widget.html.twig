{% if available.service %}<link rel="stylesheet" href="{{ asset_with_version('css/pages/widgets.css') }}">
{% for survey in availableSurveys  %}

<div class="col-12 col-md-6 col-xxxl-4">
    <div class="card">
        <div class="card-header">
            <i class="fa  fa-comment"></i> Tilgjenglige undersøkelser
        </div>

        <div class="card-body table-responsive">
            <table class="table table-striped mb-0">
                <tr>
                    <th>Ubesvarte</th>
                </tr>
                    <tr>
                        <td class="d-flex">
                            <a class="ml-2" href="{{ path('survey_show_team',{'id':survey.id}) }}" target="_blank"> {{ survey.name }}
                            </a>


                        </td>
                    </tr>
            </table>
        </div>
    </div>
</div>


    <div class="card text-center">
        <div class="card-header">
            Featured
        </div>
        <div class="card-body">
            <h5 class="card-title">Special title treatment</h5>
            <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
            <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
        <div class="card-footer text-muted">
            2 days ago
        </div>
    </div>
{% endfor %}

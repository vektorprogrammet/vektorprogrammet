<div class="row text-center">
    <h2>Våre sponsorer og partnere</h2>
    {% for sponsor in get_sponsors() %}
        {% if  loop.index0%3 == 0 %} <div class="row"> {% endif %}
            <div class="small-4 medium-4 large-4 columns">
                <a href="{{ sponsor.url }}">
                    <img src="{{ asset(sponsor.logoImagePath) }}">
                </a>
            </div>
        {% if  loop.index0%3 == 0 %}</div>{% endif %}
    {% endfor %}
</div>
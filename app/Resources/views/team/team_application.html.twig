{% extends 'base.html.twig' %}
{% block body %}
    <div class="row">
        <div class="small-12 columns">
            {% include 'common/flash.html.twig' %}
        </div>
    </div>
    <div class="row">
        <div class="small-12 columns">
            <h1>Søk {{ team.name }} - {{ team.department }}</h1>
        </div>

        {{ form_start(form) }}
        <div class="small-8 columns">
            {{ form_row(form.name) }}
            {{ form_row(form.email) }}
            {{ form_row(form.phone) }}
            {{ form_row(form.yearOfStudy) }}
            {{ form_row(form.fieldOfStudy) }}
            {{ form_row(form.biography) }}
            {{ form_row(form.motivationText) }}
            <button>Send</button>
            <br>
            <br>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}

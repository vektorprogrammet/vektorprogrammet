<div class="row">
    <div class="col-12">

        <h1 class="text-dark-blue"> {{ user.firstName }} {{ user.lastName }} </h1>
        <h6 class="subheader">
            {% for r in user.roles %}
                {{ r.name | replace({
                    'User': 'Assistent',
                    'Admin': 'Teammedlem',
                    'Superadmin': 'Teamleder',
                    'Highest admin': 'Admin'
                }) }}
            {% endfor %}
        </h6>

    </div>
</div>

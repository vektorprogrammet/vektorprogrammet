{% extends 'adminBase.html.twig' %}

{% block body %}
    <h1 class="text-center">Denne siden skal inneholde allokeringsappen</h1>
    <div class="row">
        <div class="large-12">
        </div>
        <table>
            <tr>
                <th>Navn</th>
                <th>Skole</th>
                <th>Ukedag</th>
                <th>Bolk</th>
            </tr>
            {% for application in applications %}
                <tr>
                    <td>{{ application.user.firstName }} {{ application.user.lastName }}</td>
                    <td><select name="isAttending" id="isAttending">
                            {% for school in allocations %}
                                <option value="school">{{ school.school.name }}</option>
                            {% endfor %}
                        </select></td>
                    <td><select name="isAttending" id="isAttending"></select></td>
                    <td><select name="isAttending" id="isAttending"></select></td>
                </tr>
            {% endfor %}
        </table>
        <button class="button large">Bekreft</button>
        <button class="button large">Tilbakestill</button>
        <button class="button large">Tilbake</button>
    </div>


{% endblock %}
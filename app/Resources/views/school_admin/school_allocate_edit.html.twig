{% extends 'base.html.twig' %}

{% block body %}

        <div class="large-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
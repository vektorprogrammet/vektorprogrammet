{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset_with_version('css/pages/parents.css') }}">
{% endblock %}

{% block title %}
    Påmelding
{% endblock %}


{% block body %}
    <div class="container-fluid">
        <div class="parent-assignment-page content">
            <header class="my-5 row content">
                <div class="col-md-12">
                    <h1 class="text-center header-title text-dark-blue">Påmelding på kurs med {{ parentCourse.speaker }}</h1>
                    <p class="text-center tiny-title">{{ parentCourse.date | date('d.m.y') }} klokken {{ parentCourse.date | date('H:i') }}</p>
                </div>
            </header>
            <section class="col space">
                <br>
                <div class="mx-auto">
                    {{ form_start(form) }}
                    <div class="card ">
                        <div class="card-header">
                            <h3>Meld deg på!</h3>
                        </div>
                        <div class="card-body">
                            {{ form_row(form.navn) }}
                            {{ form_row(form.epost ) }}
                        </div>
                        <div class="card-body">
                            {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) }}
                        </div>
                        <div class="hide">
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </section>
        </div>
    </div>

{%  endblock %}

<?php

namespace AppBundle\Tests\Controller;

use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

class CertificateControllerTest extends WebTestCase {

    public function testShow() {
//
//        $client = static::createClient(array(), array(
//			'PHP_AUTH_USER' => 'petjo',
//			'PHP_AUTH_PW'   => '1234',
//		));
//        $crawler = $client->request('GET', '/attest');
//
//		// Assert that we have the correct page and data
//		$this->assertContains( 'Attester', $client->getResponse()->getContent() );
//		$this->assertContains( 'Navn', $client->getResponse()->getContent() );
//		$this->assertContains( 'Avdeling', $client->getResponse()->getContent() );
//
//
//		// Check the count for the different variables
//		$this->assertEquals( 1, $crawler->filter('html:contains("Avdeling")')->count() );
//		$this->assertEquals( 1, $crawler->filter('html:contains("Navn")')->count() );
//		$this->assertEquals( 1, $crawler->filter('html:contains("Petter Johansen")')->count() );
//		$this->assertEquals( 1, $crawler->filter('html:contains("Ida Andreassen")')->count() );
//
//		// Assert a specific 200 status code
//		$this->assertEquals( 200, $client->getResponse()->getStatusCode() );
//
    }
	
	/*
	Requires JQuery interaction, Symfony2 does not support that
	
	Phpunit was designed to test the PHP language, have to use another tool to test these. 
	
	public function testDeleteAction() {}
	public function testRequestAction() {}
	
	*/
	
}
